# Database Connection URL
DATABASE_URL=postgresql://user:password@host:port/database

# API Configuration (Optional, defaults to relative path in production)
VITE_API_URL=

# Port (Optional, defaults to 3000)
PORT=3000
